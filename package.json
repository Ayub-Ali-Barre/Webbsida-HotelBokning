{
  "name": "webbsida-hotelbokning",
  "version": "1.0.0",
  "private": true,
  "description": "Static hotel booking demo site",
  "scripts": {
    "check-syntax": "node --check JS/*.js",
    "lint": "eslint 'JS/**/*.js' --ext .js",
    "format:check": "prettier --check \"**/*.{js,css,html}\"",
    "format:fix": "prettier --write \"**/*.{js,css,html}\""
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "prettier": "^2.8.8"
  }
}
